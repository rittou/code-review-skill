interface:
  display_name: "Shopware Review"
  short_description: "Review Shopware code or build isolated QA environments"
  default_prompt: "Use $shopware-review to either review this Shopware code change with tests or build an isolated Shopware dev environment and return the environment access details."
